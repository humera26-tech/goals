-- SQL script to create the digielevate database
-- Run this as a PostgreSQL superuser (usually 'postgres')

-- Connect to PostgreSQL default database first, then run:
CREATE DATABASE digielevate;

-- Optional: Set encoding and other options
-- CREATE DATABASE digielevate
--     WITH 
--     OWNER = postgres
--     ENCODING = 'UTF8'
--     LC_COLLATE = 'en_US.UTF-8'
--     LC_CTYPE = 'en_US.UTF-8'
--     TABLESPACE = pg_default
--     CONNECTION LIMIT = -1;
